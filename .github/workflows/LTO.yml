name: 🛠 Build Kernel - Proton Clang 11 (LTO Disabled)

on:
  workflow_dispatch:

jobs:
  build:
    runs-on: ubuntu-latest

    steps:
    - name: 📥 Checkout Kernel Source
      uses: actions/checkout@v4
      with:
        repository: Ponleuz12/kernel_realme_sm4250
        ref: stable
        path: kernel

    - name: 🔧 Install Dependencies
      run: |
        sudo apt update
        sudo apt install -y bc bison flex libssl-dev make \
          gcc clang curl git libncurses-dev lld

    - name: 📦 Download Proton Clang 11.0.1
      run: |
        sudo apt install -y zstd  # ✅ REQUIRED to extract .zst
        mkdir clang
        curl -LO https://github.com/kdrag0n/proton-clang/releases/download/2021.11.14/proton-clang-20211114.tar.zst
        tar --use-compress-program=unzstd -xvf proton-clang-20211114.tar.zst -C clang --strip-components=1
        
    - name: 🧰 Setup Environment
      run: |
        echo "CLANG_PATH=$(pwd)/clang/bin" >> $GITHUB_ENV
        echo "KERNEL_DIR=$(pwd)/kernel" >> $GITHUB_ENV

    - name: 🛠 Build Kernel
      working-directory: ${{ env.KERNEL_DIR }}
      run: |
        export ARCH=arm64
        export SUBARCH=arm64
        export CLANG_TRIPLE=aarch64-linux-gnu-
        export CROSS_COMPILE=aarch64-linux-gnu-
        export CROSS_COMPILE_COMPAT=arm-linux-gnueabi-
        export PATH=${{ env.CLANG_PATH }}:$PATH

        make O=out stock_defconfig
        make -j$(nproc --all) O=out \
          CC=clang \
          CROSS_COMPILE=aarch64-linux-gnu- \
          CROSS_COMPILE_COMPAT=arm-linux-gnueabi- \
          LD=ld.lld \
          AR=llvm-ar \
          NM=llvm-nm \
          OBJCOPY=llvm-objcopy \
          OBJDUMP=llvm-objdump \
          STRIP=llvm-strip

    - name: 📤 Upload zImage
      uses: actions/upload-artifact@v4
      with:
        name: Kernel Image
        path: kernel/out/arch/arm64/boot/Image.gz-dtb
